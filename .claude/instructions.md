# shop-guil — Claude Instructions

**Project path:** `D:\The Brain\raw\projects\shop-guil`

## Shell conventions

- Scripts use `Path(__file__).parent` for output paths — run from The Brain root with a relative path, no `cd` needed.
- Playwright uses system Edge (`channel="msedge"`) — no browser download required.
- Firebase credentials are in `service-account.json` (gitignored).

## Common commands

| Task | Command (from D:\The Brain) |
|------|---------|
| Run scraper | `uv run python raw/projects/shop-guil/scrape.py` |
